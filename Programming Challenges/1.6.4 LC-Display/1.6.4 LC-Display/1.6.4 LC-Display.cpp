#include <iostream>
#include <cmath>
#include <string>

using namespace std;

int main()
{
	int s, n, cont = 7;
	string str;

	do{
		cin >> s >> n;

		char numLCD[s + 2][(2 * s) + 3];
		int numI[8] = { 0 }, j = 8;
		str = to_string(n);


	} while (s != 0 && n != 0)
	return 0;
}

